policy: "MlpPolicy"

# Core SAC
learning_rate: 3.0e-4
buffer_size: 2000000
learning_starts: 250000
batch_size: 1024
tau: 0.005
gamma: 0.99
train_freq: 1
gradient_steps: 2

# Entropy
ent_coef: "auto"

# Policy network
policy_kwargs:
  net_arch: [256, 256, 256]

# Misc
verbose: 1